Front

1. When changing a product, you need to click the colored circle, which will update the sizes and price.
2. In the "Select Product" modal, the category name doesn't change when changing the language.
3. In the "Select Product" modal, the product information doesn't fit in the pop-up window (I'm wondering if the ? icon could be made a separate button to open the modal when clicked).
4. On the main page, when adding a quantity, change the amount (like in the shopping cart).
5. If, for example, 25 items are in stock for one size, but 10 items are in stock for another, i.e., if you select 25 items for the first product and switch to the 10-item size, the 25-item quantity is saved and added to the cart.
6. The cart isn't linked to the actual quantity; you can buy 100 items, but only have 10 items in stock.
7. I need to make it so that one item of each size is added to the cart as one item. Currently, I can select 10 items of size S and add them to the cart 10 times.
8. Checkout Modal:
   8.1 Make the modal's close cross larger.
   8.2 When the modal is open, hide the Menu and Cart buttons (and darken the overall background).
   8.3 Set Pickup Point as the default.
   8.4 Add the default "BTS EXPRESS" radio button to the Delivery Method section.

Admin

1. Add the ability to set a product as default in products (if there are multiple products). Currently, the most recently added one is loaded by default.
2. When editing a print, clicking the "Active Status" checkbox doesn't respond; it only works in the surrounding white space.
